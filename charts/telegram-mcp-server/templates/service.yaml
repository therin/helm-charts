apiVersion: v1
kind: Service
metadata:
  name: {{ include "telegram-mcp-server.fullname" . }}
  labels:
    {{- include "telegram-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "telegram-mcp-server.selectorLabels" . | nindent 4 }}
