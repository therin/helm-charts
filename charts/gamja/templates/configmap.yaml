---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
data:
  config.json: |
    {
      "server": {
        "url": {{ .Values.config.server.url | quote }},
        "autoconnect": {{ .Values.config.server.autoconnect }},
        "auth": {{ .Values.config.server.auth | quote }}
      }
      {{- if .Values.config.nick }},
      "nick": {{ .Values.config.nick | quote }}
      {{- end }}
      {{- if .Values.config.autojoin }},
      "autojoin": {{ .Values.config.autojoin | quote }}
      {{- end }}
    }
  nginx.conf: |
    server {
        listen 80;
        server_name _;

        # Serve gamja static files
        location / {
            proxy_pass http://127.0.0.1:8080;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # Proxy websocket to soju
        location /socket {
            proxy_pass http://{{ .Values.soju.service }}:{{ .Values.soju.websocketPort }};
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_read_timeout 86400;
        }
    }
